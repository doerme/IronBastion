{
  "name": "iron-bastion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^4.5.3",
    "vitest": "^0.34.6"
  }
}
